## Summary

- MCP package aligned with `@work-graph/cli@0.2.24` vendor runtime.
- Git snapshot on `complete_work_item` / `done` passes `target_files` for scoped code commits.
- Homepage: [workgraph.ru/en](https://workgraph.ru/en/).

## Install

```bash
npx -y @work-graph/mcp@0.2.20
```

Use with `@work-graph/cli@0.2.24` — [work-graph-cli v0.2.24](https://github.com/bvc-lang/work-graph-cli/releases/tag/v0.2.24).
