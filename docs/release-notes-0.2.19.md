# Release notes — v0.2.19

## Summary

- Fix theme toggle icon rendering: SVG fill follows `currentColor` in light/dark header.
- Hide **Prompts** nav tab when the project has no prompt rules; avoid landing on an empty Prompts view after reload.
- Homepage: [workgraph.ru/en](https://workgraph.ru/en/).

## Install

```bash
npx @work-graph/cli@0.2.19 init .
npm install
npm run workgraph:ui
```

## npm

`@work-graph/cli@0.2.19`, `@work-graph/mcp@0.2.15`.
