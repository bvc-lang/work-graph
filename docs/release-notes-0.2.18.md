# Release notes — v0.2.18

## Summary

- Fix npm update check on **Windows**: `npm view` fallback runs via shell so Settings → About can reach the registry.
- Homepage: [workgraph.ru/en](https://workgraph.ru/en/).

## Install

```bash
npx @work-graph/cli@0.2.18 init .
npm install
npm run workgraph:ui
```

## npm

`@work-graph/cli@0.2.18`, `@work-graph/mcp@0.2.14`.
