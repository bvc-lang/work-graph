# Release notes — v0.2.11

## Summary

- Monorepo sync: intent/semantic planes, UI backlog, public site docs, and MCP tooling updates vendored into `@work-graph/cli`.
- Repository line-ending policy (`.gitattributes`, LF) so BVC/work-item parsers behave consistently on Windows.
- npm **Homepage**: [workgraph.ru/en](https://workgraph.ru/en/).

## Install

```bash
npx @work-graph/cli@0.2.11 init .
npm install
npm run workgraph:ui
```

## npm

`@work-graph/cli@0.2.11`, `@work-graph/mcp@0.2.8`.
