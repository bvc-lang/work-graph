#Задача_implement_daemon_watch_mode_smoke<[
Базис:
  Одиночный tick (daemon:once) покрыт тестами; непрерывный watch без regression gate.
Вектор:
  npm run daemon:watch — короткий smoke с таймаутом, фикстурой и проверкой work/daemon-audit.jsonl.
Цель:
  Режим watch не ломается между релизами.
Проверки:
  Smoke проходит в npm run test:deterministic
  Скрипт описан в package.json

Метки:
  atom.profile: work_item
  migration.strategy: rebuild
  trace.status: verified
  work.department: agent-platform
  work.depends_on: phase-2-workgraph-os-daemon
  work.id: implement-daemon-watch-mode-smoke
  work.next_action: просмотреть и перевести в ready
  work.owner_role: system_architect
  work.priority: medium
  work.risk: medium
  work.status: done
  work.target_files: src/workGraphDaemonTick.mjs, tests/workGraphDaemonIntegration.test.mjs, package.json
  work.title: Режим watch не ломается между релизами.
]>

