{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "iohasc.bvc-atom-draft.v1",
  "title": "Step Atom Draft V1",
  "type": "object",
  "additionalProperties": false,
  "required": ["profile", "name", "basis", "vector", "goal"],
  "properties": {
    "profile": {
      "type": "string",
      "enum": ["charter", "charter_section", "work_item", "plan", "prompt_rule", "compiler", "trace"]
    },
    "name": {
      "type": "string",
      "pattern": "^[A-Za-zА-Яа-яЁё0-9_]+$"
    },
    "basis": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "vector": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "goal": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "labels": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "propertyNames": {
        "pattern": "^[A-Za-zА-Яа-яЁё0-9_.-]+$"
      }
    },
    "lang": {
      "type": "string",
      "enum": ["en", "ru"],
      "default": "en",
      "description": "BVC dialect for formatted atom keys (Basis/Базис). Work Graph legacy default in formatter: ru when omitted."
    },
    "checks": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "evidence": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
