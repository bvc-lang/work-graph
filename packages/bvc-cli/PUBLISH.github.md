# Maintainer notes

This repository is an export mirror of `@bvc-lang/cli`.

**Install (users):**

```bash
npm install -g @bvc-lang/cli
```

Publishing new versions is done from the Work Graph monorepo (`packages/bvc-cli/PUBLISH.md`), not from this GitHub tree.
